# Asset Inventory (from v1, archived in `/legacy-v1/public/images/`)

Verdict legend: **Keep** = reusable in v2 · **Maybe** = usable with rework · **Drop** = legacy-only, stays in the archive.

## People / brand

| Asset | What it is | Verdict |
|---|---|---|
| `new-photo.jpg` (720×720) | Casual outdoor photo, vacation-style | **Drop** for v2 — wrong register for a senior-architect site. Need a proper portrait (will request specs). |
| `my-photo.jpg` (~300px) | Older, low-res indoor photo | **Drop** — too small, dated. |
| `profile.jpeg` | Another small profile shot | **Drop** — same reasons. |
| `akram-blue-logo.png` / `akram-white-logo.png` | "AKRAM" wordmark with laptop/phone/gear clip-art | **Drop** — clip-art device icons say "freelance web dev 2018", which contradicts the v2 positioning. v2 gets a typographic mark instead. |
| `favicon.ico` / `favicon-old.ico` | Favicon derived from the old logo | **Drop** — regenerate from the v2 mark. |

## Project screenshots (~30 files: clones, food apps, agency sites)

`amazon-clone.PNG`, `tesla.png`, `movies.PNG`, `elakl.png`, `behappy-*.PNG`, `bookthechef*.PNG`, `yalla2.PNG`, `Capture*.PNG/JPG`, `next-ecommerce.png`, `ai-article-summarizer.png`, etc.

**Drop all** — they document tutorial-grade projects that undercut the AI/architecture positioning. v2 uses SVG architecture diagrams for proprietary work and live links for the new AI Lab projects. Several are also multi-MB unoptimized PNGs.

## Stock / decorative

Tech logos (`react.png`, `nodejs.png`, `css.png`, `bootstrap.jpg`, `sass.png`, …), stock photos (`computer-1209641_1920.jpg`, `purse-1478852_1920.jpg`, …), `undraw_developer_activity_bv83.png`, `develop.svg`, `man1.gif`, `tenor-unscreen.gif`.

**Drop all** — badge-wall logos and stock illustration are explicitly banned by the design rules.

## Not found anywhere (gaps to fill)

- **Resume PDF** — none in the repo. Provide one if the v2 site should link it.
- **Professional portrait** — none usable. I'll request exact dimensions/mood when the About section is built.
- **OpenGraph image** — none. Will be designed as part of v2.

## Bottom line

Nothing from v1 carries forward. v2's visual assets (wordmark, favicon, OG image, SVG diagrams) will all be built new in the site's own visual language; the only external asset needed from you is a portrait photo (and optionally a resume PDF).
